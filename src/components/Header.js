import React from 'react';
import './components/styles.css';

const Header = () => (
  <header style={{ textAlign: 'center' }}>
    <h1>Tax Loss Harvesting Dashboard</h1>
  </header>
);

export default Header;
